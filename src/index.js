

/*function isPalindrome(str) {
    const sourceString = str
        .split(" ").join('')
        .split(",").join('')
        .split("'").join('')
        .split('?').join('')
    const reversedStr = sourceString.split('').reverse().join('')
    console.log(sourceString, 'исходная строка')
    console.log(reversedStr)
    if (reversedStr.toUpperCase() === sourceString.toUpperCase()) {
        console.log('Да это полиндром!')
    }
}


const res = isPalindrome("Do geese see god?")
//console.log(res)*/

/*
const palindrome = 'TENET'

const palindrome2 = 'Казак'
*/

